# Bug Audit Report

**Generated:** June 3, 2026

This document details bugs, inconsistencies, and potential issues found during a systematic audit of the codebase.

| ID | Bug Description | Location | Severity | Recommended Fix | Status |
| --- | --- | --- | --- | --- | --- |
| **ENUM-001** | Invalid enum value for `escalationStatus`. | `server/services/complaintService.js` | **High** | Change `"Not Escalated"` to `"Normal"` to match the `Complaint` model's enum definition. | **Confirmed** |
| **VALIDATE-001** | Certificate department is validated, not assigned. | `server/middlewares/departmentAuthorizationMiddleware.js` | **Critical** | Rework middleware to automatically assign the correct department based on `certificateType` instead of validating a department sent from the client. | **Confirmed** |
| **ARCH-001** | Complaint department routing is not implemented. | `server/controllers/complaintController.js` | **Critical** | 1. Add a `department` field to the `Complaint` model. 2. Create a mapping from `complaint.category` to a department. 3. Implement logic in the `createComplaint` service to automatically assign the department. | **Confirmed** |
| **LOGIC-001** | Emergency routing only assigns the first department in a multi-department scenario. | `server/controllers/emergencyController.js` | **Medium** | For emergencies that map to multiple departments (e.g., 'Building Collapse'), the system should create notifications or tasks for all relevant departments, not just the first one in the array. | **Confirmed** |
| **ENUM-002** | `CERTIFICATE_TYPES` in `constants.js` is missing several types mentioned in the prompt. | `server/config/constants.js` | **High** | Add 'Domicile Certificate', 'Marriage Certificate', 'Senior Citizen Certificate', and 'Disability Certificate' to the `CERTIFICATE_TYPES` array and update the `CERTIFICATE_TYPE_DEPARTMENTS` mapping. | **Confirmed** |
| **ENUM-003** | `COMPLAINT_CATEGORIES` in `constants.js` is missing several types mentioned in the prompt. | `server/config/constants.js` | **Medium** | Add 'Public Property Damage' to the `COMPLAINT_CATEGORIES` array and create a corresponding department mapping. | **Confirmed** |
| **ENUM-004** | `EMERGENCY_DEPARTMENTS` in `constants.js` uses different naming than the prompt. | `server/config/constants.js` | **Low** | Align department names for consistency. E.g., change "Disaster Management Cell" to "Disaster Management Authority", "Panchayat Emergency Team" to "District Administration". | **Confirmed** |
| **VALIDATE-002** | User model has conditional validation for `village` and `municipality` based on `jurisdictionType`. | `server/models/User.js` | **Low** | This is good, but needs to be verified for all models with jurisdiction fields (`Complaint`, `Certificate`, `Emergency`, etc.) to ensure data integrity. | **Confirmed** |
| **API-001** | Complaint assignment `PATCH /:id/assign` is a separate endpoint. | `server/routes/complaintRoutes.js` | **Medium** | This should be part of the automatic department routing. Once routing is fixed, this manual assignment might become redundant or serve a different purpose (e.g., re-assignment). | **Confirmed** |
| **API-002** | Certificate `review` and `status` updates are separate endpoints. | `server/routes/certificateRoutes.js` | **Low** | This seems reasonable but could potentially be consolidated into a single, more robust status update endpoint that handles remarks and state transitions together. | **Confirmed** |
| **PERF-001** | Several models lack compound indexes for common query patterns. | `server/models/*.js` | **Medium** | Review query patterns in services (e.g., filtering by status, district, and date) and add compound indexes to the Mongoose schemas to improve query performance. | **Confirmed** |
| **STATE-001** | Frontend API client in `client/src/api/apiClient.js` has a hardcoded `baseURL`. | `client/src/api/apiClient.js` | **Low** | The `baseURL` for the API is hardcoded to `http://localhost:8000/api/v1`. This should be moved to an environment variable (`.env` file) to allow for different environments (development, staging, production). | **Confirmed** |
| **VALIDATE-003** | `certificateUploadMiddleware` allows up to 6 documents. | `server/middlewares/certificateUploadMiddleware.js` | **Low** | The validation `applyCertificateValidator` does not seem to check the number of files. This could lead to unexpected behavior if the frontend sends more files than the backend logic expects. | **Confirmed** |
| **ENUM-005** | `complaintSchema` `escalationStatus` enum has `Normal` but `complaintService` uses `Not Escalated`. | `server/models/Complaint.js`, `server/services/complaintService.js` | **High** | The service at line 315 sets status to "Not Escalated", which will cause a validation error on save. It should be "Normal". | **Confirmed** |
| **ENUM-006** | `CERTIFICATE_TYPE_DEPARTMENTS` in `constants.js` allows multiple departments for some certificates. | `server/config/constants.js` | **High** | `Residence Certificate` and `Caste Certificate` map to multiple departments. This will cause ambiguity and prevent deterministic, automatic assignment. A decision must be made to select one primary department for each type. | **Confirmed** |
| **ENUM-007** | `EMERGENCY_TYPE_DEPARTMENTS` in `constants.js` has a similar multi-department issue. | `server/config/constants.js` | **Medium** | `Flood`, `Road Accident`, `Building Collapse`, `Water Crisis`, `Animal Attack`, `Storm Damage` all map to multiple departments. The system currently just picks the first one. This needs a more robust solution for multi-agency response. | **Confirmed** |
| **DEPS-001** | Frontend dependencies `react-router-dom` and `recharts` have major version updates available. | `client/package.json` | **Low** | `react-router-dom` is on v7.15.1, but this seems to be a pre-release version. `recharts` is on v3.8.1, while latest is different. While not a bug, it's a maintenance concern. No immediate action required per instructions. | **Confirmed** |
| **DEPS-002** | Backend dependencies `express-rate-limit`, `mongoose`, `socket.io`, `vite` have major updates. | `server/package.json`, `client/package.json` | **Low** | `mongoose` is on v8.5.1. `socket.io` is on v4.8.3. These are not critically old, but worth noting. No immediate action required. | **Confirmed** |

---
*This report is auto-generated based on a static analysis of the codebase and the project requirements. Further bugs may be discovered during dynamic testing and implementation of fixes.*
